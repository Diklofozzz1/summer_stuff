import {makeStyles} from "@material-ui/core/styles";

const colorSpectrum = {
    lightBlue: ''
}

export const useStyles = makeStyles((theme) => ({
    group: {
        width: '30%',
        height: '80vh'
    },
}));